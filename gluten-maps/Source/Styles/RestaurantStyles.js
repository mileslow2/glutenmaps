import { StyleSheet } from "react-native";

export default (s = StyleSheet.create({
  text: {
    color: "#8b8b8b",
    fontSize: 30,
    paddingLeft: 17,
    paddingTop: 5,
    zIndex: 2,
    paddingBottom: 30
  }
}));
